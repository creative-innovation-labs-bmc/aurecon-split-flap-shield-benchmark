(() => {
'use strict';
const B=window.WallBench,root=B.root,cells=Array.from({length:B.ROWS},()=>Array(B.COLS)),anims=new Map();let raf=0,colonEls=[];
function mkFace(cls){const d=document.createElement('div');d.className=cls;const s=document.createElement('span');d.appendChild(s);return d}
function paint(face,state){face.classList.toggle('macro',!!state.macro);face.firstChild.textContent=state.char||' '}
function makeCell(r,c){const f=document.createElement('div');f.className='d2-flap';f.style.left=B.cellX(c)+'px';f.style.top=B.cellY(r)+'px';const top=mkFace('d2-half d2-top'),bot=mkFace('d2-half d2-bottom'),tf=mkFace('d2-half d2-top d2-moving'),bf=mkFace('d2-half d2-bottom d2-moving');f.append(top,bot,tf,bf);root.appendChild(f);const cell={f,top,bot,tf,bf,state:{char:' ',macro:false},pending:null};paint(top,cell.state);paint(bot,cell.state);return cell}
function init(){root.className='dom2d-root';for(let r=0;r<B.ROWS;r++)for(let c=0;c<B.COLS;c++)cells[r][c]=makeCell(r,c);addDividers();addColons()}
function addDividers(){[B.CENTRE_START,B.CENTRE_START+B.CENTRE_COLS].forEach(c=>{const d=document.createElement('div');d.className='zone-divider';d.style.left=(B.cellX(c)-2)+'px';root.appendChild(d)})}
function addColons(){B.COLON_GAPS.forEach(([l,r])=>B.COLON_ROWS.forEach(row=>{[['left',l],['right',r]].forEach(([side,lc])=>{const el=document.createElement('span');el.className='main-colon '+side;el.style.left=B.cellX(B.CENTRE_START+lc)+'px';el.style.top=(B.cellY(row)+35)+'px';root.appendChild(el);colonEls.push(el)})}));[[2,3],[6,3],[2,45],[6,45]].forEach(([r,c])=>{const el=document.createElement('span');el.className='mini-colon';el.style.left=(B.cellX(c)+32)+'px';el.style.top=(B.cellY(r)+25)+'px';root.appendChild(el);colonEls.push(el)})}
function setCell(r,c,next,opt={}){const cell=cells[r][c],delay=opt.delay||0,duration=opt.duration||300;if(cell.state.char===next.char&&cell.state.macro===next.macro&&!cell.pending)return;const start=performance.now()+delay;cell.pending={from:{...cell.state},to:{...next},start,duration};anims.set(cell,cell.pending);paint(cell.top,cell.state);paint(cell.bot,cell.state);paint(cell.tf,cell.state);paint(cell.bf,next);wake()}
function wake(){if(!raf)raf=requestAnimationFrame(frame)}
function frame(t){raf=0;anims.forEach((a,cell)=>{if(t<a.start)return;const p=Math.min(1,(t-a.start)/a.duration);if(p<.5){const s=Math.max(.001,1-p*2);cell.tf.style.display='block';cell.bf.style.display='none';cell.tf.style.transform=`scaleY(${s})`;cell.tf.style.opacity=String(.92-.25*p*2)}else{if(cell.top.dataset.sw!=='1'){paint(cell.top,a.to);cell.top.dataset.sw='1'}const s=Math.max(.001,(p-.5)*2);cell.tf.style.display='none';cell.bf.style.display='block';cell.bf.style.transform=`scaleY(${s})`;cell.bf.style.opacity=String(.74+.26*s)}if(p>=1){paint(cell.top,a.to);paint(cell.bot,a.to);cell.top.dataset.sw='0';cell.tf.style.display='none';cell.bf.style.display='none';cell.state={...a.to};cell.pending=null;anims.delete(cell)}});if(anims.size)raf=requestAnimationFrame(frame)}
function pulse(){colonEls.forEach(e=>e.classList.add('dim'));setTimeout(()=>colonEls.forEach(e=>e.classList.remove('dim')),150)}
B.register({init,setCell,pulseColons:pulse,setColons(){},activeCount:()=>anims.size});
})();